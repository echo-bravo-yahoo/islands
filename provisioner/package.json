{
  "name": "provisioner",
  "version": "1.0.0",
  "description": "We're using [sdm](https://github.com/gitbls/sdm) to provision islands.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "yargs": "^17.7.2"
  }
}
